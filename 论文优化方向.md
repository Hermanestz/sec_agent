# 优化方向

## 搭建Memory模块和知识库

目标：
评估指标：运行时间，API call次数

## 优化工作流

目标：在保持准确率的情况下，实现更短的运行时间
评估指标：运行时间，API call次数，模型大小

## 利用被动DNS进行枢轴分析（Pivoting）

进行基础设施关联分析：从一个已知的恶意指标（如一个可疑域名）出发，通过pDNS数据库不断跳转，以绘制出攻击者的完整基础设施图谱 。例如，从一个恶意域名evil.com出发，可以：

- 查询evil.com的历史IP地址。
- 对这些IP地址进行反向查询，找到托管在相同IP上的其他域名。
- 查询这些新发现域名的WHOIS信息，寻找共同的注册人邮箱或名称。
- 查询这些域名的权威名称服务器，再反查使用相同名称服务器的其他域名。
- 将查询到的恶意域名加入知识库，提前防御未来的潜在威胁

通过这个过程，一个孤立的告警事件可以扩展为对一个完整攻击活动的全面洞察，

## 构建关系图谱

当收到一个警报（如一个恶意域名evil-domain.com）时，分析师不再是只看到一个点，而是可以立即查询图谱，获得一个完整的视图：

- 它解析到了哪些IP地址？
- 这些IP地址还托管了哪些其他域名？（基础设施关联）
- 我们内部有哪些主机曾经查询或访问过这个域名/IP？（影响范围评估）
- 这个域名/IP是否与已知的恶意软件家族有关？（威胁定性）

## SFT微调

目标：
提高LLM综合推理阶段的能力，解决幻觉问题和可解释性问题,将通用的大语言模型（LLM）改造为一个“网络安全分析领域专家模型”

实施方法：
创建一个高质量的指令微调数据集，每一条数据包含：
"instruction": “你是一名高级网络安全分析师，请根据以下信息，分析域名 example.com 的风险等级，并提供详细的分析报告。”
"input": 包含了您系统中所有节点收集到的原始信息，如 { "dga_score": 0.95, "whois_info": {...}, "ip_reputation": [...], "web_content_analysis": {...} }
"output": 一份包含关键证据和推理过程的结构化分析报告
通过微调，大模型可以学习安全专家是如何一步步从原始数据推导出结论的，并且可以学会按照预定义的格式（如JSON）输出结果

## 测试数据集

黑样本：恶意域名可以使用abuse.cn平台的最新恶意URL，从中提取域名和ip，再经过VirusTotal，OTX威胁情报平台验证
白样本：从Alexa Top 1 Million Sites选取域名，再经过VirusTotal，OTX威胁情报平台验证

## 测试方法

分别用原方法和优化后的新方法对黑样本和白样本检测，对比检测的准确率和运行时间
